<time datetime="<%= date.iso8601 %>" class="<%= style %>" aria-label="<%= l(date, format: :long) %>"><span class="date-marker-month"><%= l(date, format: '%b') %></span><span class="date-marker-day"><%= date.day %></span></time>
