<% disable_turbo_cache %>
<div class="w-full max-w-3xl">
  <h1 class="text-3xl font-semibold text-ink"><%= t("event_plans.new.title") %></h1>
  <p class="mt-2 max-w-2xl text-base leading-7 text-quiet-note"><%= t("event_plans.new.body") %></p>
  <%= render "form", event_plan: @event_plan %>
</div>
